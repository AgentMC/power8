#summary Known issues of the project
#labels Featured

= Introduction =

This is a list of known product issues.

This list is maintained internally, if you want to report an issue that is not on list, please use [http://code.google.com/p/power8/issues/list Issues] tab of the project.

= The list of issues =

== Planned for upcoming release == 

  #  (AK) Icons for Administrative tools, Network connections and computers may not be shown (template creation required)
  #  (AK) Style for Start Menu menuitem is wrong and hardly readable on dark themes
  #  (AK) Styles for menu-buttons and simple buttons differ
  #  (MM) Installer required, msi-based auto-update should be implemented
  #  (MM) "Open all users folder" item should be disabled for non-start-menu folders
  #  (AK) Menu from menu-button may show in unapropriate position
  #  (AK) Context menu from Main button may show in unapropriate position
  #  (AK) Menu-buttons arrow-boxes are too narrow

== Other known ==

  #  Ctrl+Click may not invoke Run dialog, depending on focused control
  #  Cannot display properties of a Computer object
  #  (?) W8 immersive links aren't run properly - have to investigate, postpone to RTM
  #  (warning) investigate why the process requires Exit() call to quit 

= Done => goes to 0.3.5 =

  #  Ram drives and Removable drives not supported now, drives list is static
  #  Open/Open all users folder doesn't work for root start menu item