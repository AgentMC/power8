<UserControl x:Class="Power8.MenuedButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Name="dblBtn"
             d:DesignHeight="60" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Content="{Binding ElementName=dblBtn, Path=Item.FriendlyName}" Name="actionBtn" Grid.Column="0" Padding="0"/>
        <Button Content=">" Grid.Column="1" Padding="1,0" Click="DisplayMenu" Name="menuDropper" />
    </Grid>
    <UserControl.Resources>
        <ContextMenu x:Key="itemsList" 
                     ItemsSource="{Binding Item.Items}" 
                     ItemContainerStyle="{DynamicResource MenuItemStyle}"
                     ContextMenuOpening="ContextMenuContextMenuOpening"
                     Placement="Right"
                     PlacementTarget="{Binding MenuTarget}"/>
    </UserControl.Resources>
</UserControl>
